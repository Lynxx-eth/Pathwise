# Frontend (React + TypeScript)

Not started yet. Per the build plan:

```
npm create vite@latest . -- --template react-ts
npm install
npm install tailwindcss @tailwindcss/vite
npx shadcn@latest init
```

Build order should follow `../docs/pathwise-build-plan.md`, starting with the Courses grid (Step 3) once the backend's course-intelligence endpoint (Step 2) exists.
