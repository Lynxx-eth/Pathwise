# Backend (Python + FastAPI)

Not started yet. Per the build plan, Step 1 (auth, DB, storage) and Step 2 (course intelligence — upload, parsing, topic extraction) come first.

Suggested to start with:
```
python -m venv venv
source venv/bin/activate
pip install fastapi uvicorn sqlalchemy --break-system-packages
```
